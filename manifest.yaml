apiVersion: v1
kind: Secret
metadata:
  name: my-secret
  annotations:
    openshift.io/token-secret.value: secret
    api: key
    app: "null"
    new: "true"
    #timestamp: null
type: Opaque
stringData:
  username: username
---
apiVersion: v1
kind: Pod
metadata:
  name: pod1
  annotations:
    openshift.io/token-secret.value: secret
    api: key
    app: "null"
    #timestamp: null
spec:
  containers:
  - name: container1
    image: public.ecr.aws/docker/library/alpine
    command: ["sleep"]
    args:
    - 10h


